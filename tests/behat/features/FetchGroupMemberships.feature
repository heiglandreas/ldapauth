Feature: Fetch Group memberships in 2 different ways	Scenario: Get all groups a user belongs to via the users memberOf-Attribute		Given a group "cn=group1,dc=example,dc=com" exists		And a group "cn=group2,dc=example,dc=com" exists		And a group "cn=group3,dc=example,dc=com" exists		And a user "uid=user,dc=example,dc=com" exists		And contains the attribute "memberOf" with value "cn=group2,dc=example,dc=com"		And contains the attribute "memberOf" with value "cn=group1,dc=example,dc=com"		When I search for the groups of "uid=user,dc=example,dc=com"		Then I will get a list-object containing "cn=group1,dc=example,dc=com"		And I will get a list-object containing "cn=group2,dc=example,dc=com"	Scenario: Get all groups a user belongs to via the groups uniqueMenber-Attribute		Given a group "cn=group1,dc=example,dc=com" exists		And contains the attribute "uniqueMemberOf" with value "uid=user,dc=example,dc=com"		And a group "cn=group2,dc=example,dc=com" exists		And contains the attribute "uniqueMemberOf" with value "uid=user,dc=example,dc=com"		And a group "cn=group3,dc=example,dc=com" exists		And a user "uid=user,dc=example,dc=com" exists		When I search for the groups of "uid=user,dc=example,dc=com"		Then I will get a list-object containing "cn=group1,dc=example,dc=com"		And I will get a list-object containing "cn=group2,dc=example,dc=com"