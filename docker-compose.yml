version: "3.5"services:  openldap:    image: osixia/openldap:latest    ports:      - ${PORT_LDAP}:389      - ${PORT_LDAPS}:636    volumes:      - ./docker/50-init.ldif:/container/service/slapd/assets/config/bootstrap/ldif/custom/50-bootstrap.ldif    command: "--copy-service --loglevel debug"    environment:      LDAP_LOG_LEVEL: "${LDAP_LOG_LEVEL}"      LDAP_ADMIN_PASSWORD: "${LDAP_ADMIN_PASSWORD}"